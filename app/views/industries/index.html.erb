<div class="card-alert card-green">
<div class="card-content white-black"><p id="notice"><%= notice %></p></div>
</div>

<h1>Industries</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Descripción</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @industries.each do |industry| %>
      <tr>
        <td><%= industry.name %></td>
        <td><%= industry.description %></td>
        <td><%= link_to 'Mostrar', industry %></td>
        <td><%= link_to 'Editar', edit_industry_path(industry) %></td>
        <td><%= link_to 'Eliminar', industry, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Industry', new_industry_path %>
