<nav class="config-breadcrumb z-depth-0">
    <div class="nav-wrapper">
      <div class="col s12">
        <a href="/configuracion" class="breadcrumb">Configuración</a>
        <a href="/configuracion/departamentos" class="breadcrumb">Departamentos</a>
      </div>
    </div>
</nav>

<table class="highlight bordered">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Usuarios</th>
    </tr>
  </thead>

  <tbody>
    <% @departments.each do |department| %>
      <tr class="tr-link" data-href="<%= edit_department_path(department) %>">
        <td><%= department.name %></td>
        <td><%= department.user_id %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<div class="fixed-action-btn">
  <a class="btn-floating btn-large waves-effect waves-light red" href="<%= new_department_path %>"><i class="material-icons">add</i></a>
</div>

<script>
Materialize.toast('<%= notice %>', 3000)
</script>
